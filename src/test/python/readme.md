Uses jwcrypto which can be installed using:    
```
pip install jwcrypto
```

To run, use:
```
python <script_name>
```

Signing uses pyjwt and requires PEM keys

Encrypting uses jwcrypto and can use either JWK or PEM keys